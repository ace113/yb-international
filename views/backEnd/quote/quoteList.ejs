<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/admin/quotes">Quotes</a></li>
        <li class="breadcrumb-item active" aria-current="page">All Quotes</li>
    </ol>
</nav>
<div class="card">
    <div class="card-header bg-primary text-light">
        <h4 class="my-auto" style="float: left;">All Quotes</h4>
        <a href="/admin/quote/add" class="btn btn-info btn-sm" style="float: right">Add New</a>
    </div>
</div>
<div class="card-body bg-light text-dark">
    <table class="table table-sm table-hover table-bordered table-responsive-sm">
        <thead>
            <th>S.No.</th>
            <th>name</th>
            <th>Email</th>
            <th>Product</th>
            <th>Quantity</th>
            <th>Submitted Date</th>           
            <th width="40">&nbsp;</th>
        </thead>
        <tbody>
            <% quoteList.forEach((quote, sn=1)=> { %>
            <tr>
                <td><%= sn+1 %></td>
                <td><a href="/admin/quote/<%= quote._id %>"><%= quote.name %></a></td>
                <td><%= quote.email %></td>
                <td><%= quote.product %></td>
                <td><%= quote.quantity %></td>
                <td><%= quote.createdAt.toISOString().split('T')[0] %></td>               
                <td>
                    <div class="dropdown">
                        <button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button"
                            data-toggle="dropdown">
                            <i class="fa fa-cog"></i>
                        </button>
                        <ul class="dropdown-menu">
                            <li>
                                <form action="/admin/quote/edit/<%= quote._id %>?_method=DELETE" method="POST">
                                    <button class="btn btn-primary btn-sm dropdown-item" type="submit"><i
                                            class="fas fa-trash"></i> Delete</button>
                                </form>
                            </li>
                            <li>
                                <a class="btn btn-primary btn-sm dropdown-item"
                                    href="/admin/quote/edit/<%= quote._id %>"><i class="fas fa-edit"></i> Edit</a>
                            </li>
                        </ul>
                    </div>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</div>